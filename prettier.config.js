module.exports = {
  semi: true, // Añade punto y coma al final de las declaraciones
  trailingComma: 'es5', // Añade comas al final de listas y objetos cuando es compatible con ES5 (arrays, objetos, etc.)
  singleQuote: true, // Usa comillas simples en lugar de dobles
  jsxSingleQuote: false, // Usa comillas dobles en JSX
  printWidth: 500, // Limita la longitud de las líneas a 80 caracteres (puedes ajustarlo a tu preferencia)
  tabWidth: 2, // Define el ancho de tabulación como 2 espacios
  useTabs: false,  // Usa espacios en lugar de tabulaciones
  proseWrap: 'never', // Evita que Prettier divida cadenas de texto largas
}
